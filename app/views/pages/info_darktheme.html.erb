<% content_for(:description) do %>
  Mes créations actuelles sont le fruit de vingt années de pratique et de recherche. Mes céramiques singulières sont en faïence, décorées au pinceau. Ce décor tend vers la calligraphie et laisse place à l'imagination et la représentation. Ma calligraphie est pourvoyeuse de paréidolie.
<% end %>
<% content_for(:title_tag) do %>
  Hélène Raynal, céramiste contemporaine à Saint-Quentin-la-Poterie
<% end %>

<%= render 'shared/info_pics' %>

<div class="container">
  <div class="row">

  <!-- LAPTOP -->
    <div class="hidden-xs hidden-sm col-sm-12">
      <div class="wrapper-darktheme">
        <%= photo_update('avatarphoto', 35, '', '', 15, '.avatarpic', "Mise à jour photo perso")%>
        <%= @avatarpictag %>

        <% if user_signed_in? && current_user.admin %>
          <% article_typing("info", "horizontal", "markdown1", "live-text0", "input0") %>
          <% @markdown = @markdown1 %>
          <%= render 'shared/articletyping' %>
        <% else %>
            <div id="live-text-darktheme">
              <% article_display("info", "horizontal") %>
              <%= @markdown.html_safe %>
            </div>
        <% end %>
      </div>
    </div>

  <!-- MOBILE -->
   <div class="col-xs-12 hidden-md hidden-lg mobile-info">
      <div class="wrapper-mobile-darktheme">
        <div class="img-avatar-mobile"><%= @avatarpictagmobile %></div>
        <%= @markdown.html_safe %>
      </div>
    </div>

  </div>
</div>

<% content_for(:after_js) do %>
  <script type="application/ld+json">
    {
      "@context": "http://schema.org",
      "@type": "ArtGallery",
      "image": ["<%=request.original_url%><%= image_path ENV['HOMEPIC']%>"],
      "@id": "<%=request.original_url%>",
      "name": "Les céramiques artisanales d'Hélène Raynal",
      "telephone": "<%= ENV['TELEPHONE']%>",
      "address": {
        "@type": "PostalAddress",
        "streetAddress": "12 rue de la fontaine",
        "addressLocality": "Saint-Quentin-la-Poterie",
        "addressRegion": "Gard",
        "postalCode": "30700",
        "addressCountry": "FR"
      },
      "geo": {
        "@type": "GeoCoordinates",
        "latitude": <%= ENV['LAT'].to_f %>,
        "longitude": <%= ENV['LAT'].to_f %>
      },
      "url": "<%=request.original_url%>",
      "openingHoursSpecification": [
        {
          "@type": "OpeningHoursSpecification",
          "dayOfWeek": ["Monday", "Tuesday", "Wednesday", "Thursday", "Friday"],
          "opens": "10:00",
          "closes": "17:00"
        },
        {
          "@type": "OpeningHoursSpecification",
          "dayOfWeek": ["Saturday","Sunday"],
          "opens": "closed",
          "closes": "closed"
        }
      ],
     "priceRange":"20€ à 500€"
    }
  </script>
<% end %>
